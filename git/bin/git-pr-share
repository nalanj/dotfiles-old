#!/bin/bash

# Creates a new PR branch and adds the given rev to it

set -e

if [[ "$#" -ne 1 ]]; then
    echo "Usage: git pr-share [name]"
    exit
fi

name="pr/$1"

git push origin $name:$1

